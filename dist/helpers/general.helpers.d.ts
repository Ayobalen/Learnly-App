export declare class Helpers {
    randomStringGen(length: number): Promise<string>;
    convertHoursToMinutes(timeInHours: any): Promise<number>;
    randomNumberGen(length?: number): Promise<string>;
}
